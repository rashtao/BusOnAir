package domain;

import java.util.Comparator;

import myShortest.Direction;

public class TransientStopComparator implements Comparator<TransientStop>{

	@Override
	public int compare(TransientStop o1, TransientStop o2) {

		return 0;
	}

}
